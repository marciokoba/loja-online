name: CI

on:
pull_request:
  branches: [ main ]

  jobs:
    testes:
      runs-on: Sequoia 15.7.3
      steps:
        - uses: actions/checkout@v4
        - name: Instalar dependências
        run: npm install
        - name: Rodar os testes
        run: npm test